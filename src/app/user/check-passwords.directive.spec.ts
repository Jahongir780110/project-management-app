import { CheckPasswordsDirective } from './check-passwords.directive';

describe('CheckPasswordsDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckPasswordsDirective();
    expect(directive).toBeTruthy();
  });
});
